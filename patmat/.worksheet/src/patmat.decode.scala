package patmat

object decode {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(63); val res$0 = 
  println(Huffman.decodeSecret);System.out.println("""res0: <error> = """ + $show(res$0))}
}
