package patmat

import Huffman._

object decoder {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(83); 
  println(Huffman.decodedSecret)}
  
  //encode(createCodeTree(List()))(List('b'))
}
