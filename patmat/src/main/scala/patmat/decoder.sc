package patmat

import Huffman._

object decoder {
  println(Huffman.decodedSecret)                  //> List(h, u, f, f, m, a, n, e, s, t, c, o, o, l)
  
  //encode(createCodeTree(List()))(List('b'))
}